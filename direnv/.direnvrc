# use ruby [version]
use_ruby() {
  # source /usr/local/opt/chruby/share/chruby/chruby.sh
  # source $USER/.nix-profile/chruby.sh

  local ver=$1

  if [[ -z $ver ]] && [[ -f .ruby-version ]]; then
    ver=$(cat .ruby-version)
  fi

  if [[ ! -z $ver ]]; then
    chruby $ver
  fi
}
